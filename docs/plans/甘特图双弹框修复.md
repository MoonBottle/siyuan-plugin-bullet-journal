# 甘特图事项详情弹框修复

## 一、双弹框/多弹框问题（已修复）

用户反馈：点击甘特图中的事项时，会弹出多个「事项详情」弹框（2 个、4 个等），需多次关闭。问题非必现。

**根因**：`onTaskClick` 的 handler 被重复绑定。Tab 切换时，`destroy()` 仅执行 `innerHTML = ''`，可能未触发 Vue 的 `onUnmounted`，导致旧 handler 未解绑；再次进入甘特图时 `onMounted` 再次 attach，handler 累积。dhtmlx-gantt 支持同一事件绑定多个 handler，会全部执行。

**修复**：
1. **GanttView**：在 `attachEvent` 前先 `detachEvent` 已存在的 handler（利用模块级变量保存的 ID），确保同一时刻最多一个 handler
2. **dialog.ts**：`showEventDetailModal` 单例守卫，打开新弹框前关闭已存在的同类型弹框

---

## 二、显示格式不一致问题

用户反馈：从文档点开的事项详情弹框（图一）与从甘特图点开的（图二）显示格式不同。

**根因**：存在两套实现，数据结构和格式化逻辑不同：

| 来源 | 函数 | 时间格式 | 按钮 |
|------|------|----------|------|
| 文档（index.ts 右键菜单） | `showItemDetailModal` | `2026-03-09 · 10:00~12:00`（formatDateLabel + formatTimeRange） | 取消、在日历中查看、打开文档 |
| 甘特图 / 日历 | `showEventDetailModal` | `2026/03/09 10:00:00 - 2026/03/09 12:00:00`（formatDateTime） | 取消、打开文档 |

**修复思路**：让 `showEventDetailModal` 采用与 `showItemDetailModal` 相同的展示逻辑。

### 修改 [src/utils/dialog.ts](src/utils/dialog.ts) 中的 `showEventDetailModal`

**1. 时间格式统一**

将当前逻辑：
```typescript
let timeDisplay = '';
if (end && start !== end) {
  timeDisplay = `${formatDateTime(start, allDay)} - ${formatDateTime(end, allDay)}`;
} else {
  timeDisplay = formatDateTime(start, allDay);
}
// 使用 timeDisplay 直接显示
```

改为与 `showItemDetailModal` 一致：
```typescript
const dateStr = props.date || (typeof start === 'string' ? start.split(' ')[0] : '') || start;
const dateLabel = formatDateLabel(dateStr, t('todo').today, t('todo').tomorrow);
const timeRange = formatTimeRange(start, end);
const timeText = `${dateLabel}${timeRange ? ' · ' + timeRange : ''}`;
// 事项卡片中使用 timeText
```

**2. 按钮统一**

在 `createButtons` 中增加「在日历中查看」：
```typescript
createButtons([
  { text: t('common').cancel, class: 'b3-button--outline', action: 'close' },
  { text: t('todo').viewInCalendar, class: 'b3-button--outline', action: 'open-calendar' },
  { text: t('todo').openDoc, class: 'b3-button--text', action: 'open-doc' },
])
```

**3. 绑定 open-calendar 事件**

在按钮点击处理中增加：
```typescript
} else if (action === 'open-calendar') {
  const dateStr = props.date || (typeof event.start === 'string' ? event.start.split(' ')[0] : '') || event.start;
  if (plugin && (plugin as any).openCustomTab) {
    (plugin as any).openCustomTab(TAB_TYPES.CALENDAR, { initialDate: dateStr });
  }
  dialog.destroy();
}
```

并在函数开头添加 `const plugin = usePlugin();`。

---

## 双弹框修复（已完成）

单例守卫已实现：打开新弹框前关闭已存在的 `lastEventDetailDialog`，并通过 `destroyCallback` 维护引用。

## 验证建议

1. 从文档右键「查看事项详情」与从甘特图点击事项，弹框时间格式一致（如 `2026-03-09 · 10:00~12:00`）
2. 甘特图/日历弹框底部有三个按钮：取消、在日历中查看、打开文档
3. 点击「在日历中查看」可正确跳转到日历并定位日期

---

## 三、动态导入导致的代码分割问题（已修复）

**问题**：使用 `await import()` 动态导入时，Vite 会生成代码分割的 chunk 文件（如 `recurringParser-xxx.js`），这些文件放在 `assets/` 目录下。但思源插件环境加载这些动态 chunk 时会报错：
```
Cannot find module './assets/recurringParser-xxx.js'
```

**根因**：思源插件的加载机制不支持动态导入产生的代码分割。

**修复**：将所有 `await import()` 改为静态 `import`：

| 文件 | 修改前 | 修改后 |
|------|--------|--------|
| `src/utils/dialog.ts` | `const { getNextOccurrenceDate } = await import('@/parser/recurringParser')` | `import { getNextOccurrenceDate, ... } from '@/parser/recurringParser'` |
| `src/utils/recurringService.ts` | `const { createNextOccurrence } = await import('@/services/recurringService')` | 移除（本来就是同文件） |

**验证**：构建后检查 `assets/` 目录，确认没有动态 chunk 文件生成，所有代码打包在 `index.js` 中。

---

## 四、UI 组件样式统一（已修复）

**问题**：提醒设置和重复设置对话框中的 checkbox 使用原生 `<input type="checkbox">`，样式异常。

**修复**：统一使用项目中的 `SySwitch` 组件（思源主题封装）：

| 文件 | 修改 |
|------|------|
| `src/components/dialog/ReminderSettingDialog.vue` | `<input type="checkbox" class="b3-switch">` → `<SySwitch v-model="enabled" />` |
| `src/components/dialog/RecurringSettingDialog.vue` | `<input type="checkbox" class="b3-switch">` → `<SySwitch v-model="enabled" />` |

**验证**：打开 `/tx` 和 `/cf` 命令设置的对话框，checkbox 样式应与思源主题一致。


---

## 五、弹框 ESC 键关闭问题（已修复）

**问题**：通过斜杠命令（`/tx`、`/cf`）打开的弹框，第一次按 ESC 键无法关闭，需要先点击弹框内部获取焦点。

**根因**：弹框打开时焦点仍在编辑器，不在弹框内，所以 ESC 键事件未被弹框捕获。

**修复**：在 `showReminderSettingDialog` 和 `showRecurringSettingDialog` 中，弹框创建后自动聚焦到内部第一个可交互元素（按钮/输入框等）：

```typescript
// 自动聚焦到弹框内，使 ESC 键立即生效
requestAnimationFrame(() => {
  const focusableEl = dialog.element.querySelector('button, input, [tabindex]:not([tabindex="-1"])') as HTMLElement;
  if (focusableEl) {
    focusableEl.focus();
  }
});
```

这样弹框打开后焦点立即在内部，ESC 键可以立即关闭弹框。


---

## 六、完成重复事项自动创建下一次（已完成）

**需求**：设置重复后，当完成事项时，应该自动创建下一次的重复事项。

**实现**：在以下三个地方的完成事项逻辑中添加自动创建下一次事项：

1. **TodoSidebar.vue** - Todo 面板中点击完成按钮
2. **GanttView.vue** - 甘特图中右键菜单完成
3. **CalendarView.vue** - 日历中右键菜单完成

**实现代码**：
```typescript
// 标记事项完成
const tag = getStatusTag('completed');
const success = await updateBlockContent(item.blockId, tag);

// 如果有重复规则，自动创建下一次事项
if (success && item.repeatRule && plugin) {
  await createNextOccurrence(plugin, item as Item);
}
```

**注意事项**：
- 只在成功标记完成且事项有 `repeatRule` 时才创建下一次
- 调用 `createNextOccurrence` 服务函数，它会处理结束条件（🔚日期、🔢次数）
- 如果已达到结束条件，则不会创建下一次


---

## 六、完成重复事项自动创建下一次（已完成）

**需求**：设置重复后，当完成事项时，应该自动创建下一次的重复事项。

**实现**：在以下五个地方的完成事项逻辑中添加自动创建下一次事项：

1. **TodoSidebar.vue** - Todo 面板中点击完成按钮
2. **GanttView.vue** - 甘特图中右键菜单完成
3. **CalendarView.vue** - 日历中右键菜单完成
4. **PomodoroActiveTimer.vue** - 番茄钟面板中点击完成按钮
5. **slashCommands.ts** - 斜杠命令 `/done` 标记完成

**实现代码**：
```typescript
// 标记事项完成
const success = await updateBlockContent(blockId, tag);

// 如果有重复规则，自动创建下一次事项
if (success && item?.repeatRule && plugin) {
  await createNextOccurrence(plugin, item);
}
```

**注意事项**：
- 只在成功标记完成且事项有 `repeatRule` 时才创建下一次
- 调用 `createNextOccurrence` 服务函数，它会处理结束条件（🔚日期、🔢次数）
- 如果已达到结束条件，则不会创建下一次
